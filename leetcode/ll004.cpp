#include "lc.hpp"
class Solution {
public:
    bool findNumberIn2DArray(vector<vector<int>>& matrix, int target) {
        if(!matrix.size()||!matrix[0].size())return false;
        int s1 = 0, s2 = 0;
        int l1 = matrix.size()-1, l2 = matrix[0].size()-1;
        while(s1!=l1&&s2!=l2){
            
        }
    }
};